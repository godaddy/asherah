<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.godaddy.asherah</groupId>
  <artifactId>native-app</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>native-app</name>
  <description>GraalVM Native Image sample for Asherah using JDK crypto provider</description>

  <properties>
    <java.version>22</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>

    <!-- Asherah dependencies -->
    <appencryption.version>0.3.4</appencryption.version>

    <!-- Plugin versions -->
    <maven.compiler.version>3.13.0</maven.compiler.version>
    <maven.jar.version>3.4.2</maven.jar.version>
    <native.maven.plugin.version>0.10.4</native.maven.plugin.version>

    <!-- Logging -->
    <slf4j.version>2.0.16</slf4j.version>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.version}</version>
        <configuration>
          <release>${java.version}</release>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven.jar.version}</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>com.godaddy.asherah.nativeapp.App</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

      <!-- GraalVM Native Image Plugin -->
      <plugin>
        <groupId>org.graalvm.buildtools</groupId>
        <artifactId>native-maven-plugin</artifactId>
        <version>${native.maven.plugin.version}</version>
        <extensions>true</extensions>
        <executions>
          <execution>
            <id>build-native</id>
            <goals>
              <goal>compile-no-fork</goal>
            </goals>
            <phase>package</phase>
          </execution>
        </executions>
        <configuration>
          <imageName>asherah-native</imageName>
          <mainClass>com.godaddy.asherah.nativeapp.App</mainClass>
          <buildArgs>
            <buildArg>--no-fallback</buildArg>
            <buildArg>--enable-url-protocols=http,https</buildArg>
            <buildArg>-H:+ReportExceptionStackTraces</buildArg>
            <!-- Initialize crypto at build time for faster startup -->
            <buildArg>--initialize-at-build-time=org.slf4j</buildArg>
            <!-- FFM native access for secure memory -->
            <buildArg>--enable-native-access=ALL-UNNAMED</buildArg>
            <!-- Enable FFM foreign API support for secure memory -->
            <buildArg>-H:+ForeignAPISupport</buildArg>
            <!-- Unlock experimental foreign API support -->
            <buildArg>-H:+UnlockExperimentalVMOptions</buildArg>
          </buildArgs>
          <metadataRepository>
            <enabled>true</enabled>
          </metadataRepository>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- Asherah App Encryption - exclude BouncyCastle only -->
    <dependency>
      <groupId>com.godaddy.asherah</groupId>
      <artifactId>appencryption</artifactId>
      <version>${appencryption.version}</version>
      <exclusions>
        <!-- Exclude BouncyCastle - we use JDK crypto only -->
        <exclusion>
          <groupId>org.bouncycastle</groupId>
          <artifactId>bcprov-jdk18on</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- Simple logging for native image -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
  </dependencies>

  <profiles>
    <!-- Profile for running as regular JAR -->
    <profile>
      <id>jar</id>
      <build>
        <plugins>
          <plugin>
            <artifactId>maven-assembly-plugin</artifactId>
            <version>3.7.1</version>
            <configuration>
              <archive>
                <manifest>
                  <mainClass>com.godaddy.asherah.nativeapp.App</mainClass>
                </manifest>
              </archive>
              <descriptorRefs>
                <descriptorRef>jar-with-dependencies</descriptorRef>
              </descriptorRefs>
            </configuration>
            <executions>
              <execution>
                <id>make-assembly</id>
                <phase>package</phase>
                <goals>
                  <goal>single</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>

